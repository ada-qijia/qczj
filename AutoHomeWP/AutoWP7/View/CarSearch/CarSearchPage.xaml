<phone:PhoneApplicationPage 
    x:Class="AutoWP7.View.CarSearch.CarSearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:p="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:MyPhoneControls;assembly=MyPhoneControls"
    xmlns:uc="clr-namespace:AutoWP7.UcControl"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="480"
    shell:SystemTray.IsVisible="False">

    <phone:PhoneApplicationPage.Resources>

    </phone:PhoneApplicationPage.Resources>

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="pageTitle" FontSize="56" Foreground="{StaticResource App_Theme_Color}" Margin="24,12,0,0"
                   Text="条件筛选"/>

        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="24,10,24,0">
            <ScrollViewer>
                <ItemsControl x:Name="filterListBox">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <uc:CarFilderFilterItem FilterName="{Binding key}" 
                                                    Loaded="Filter_Loaded"
                                                    Tap="Filter_Tap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <!--<StackPanel>
                    <uc:CarFilderFilterItem FilterName="品牌" Tap="FilterItem_Tap" Tag="brand"/>
                    <uc:CarFilderFilterItem FilterName="价格" Tap="FilterItem_Tap" Tag="price"/>
                    <uc:CarFilderFilterItem FilterName="级别" Tap="FilterItem_Tap" Tag=""/>
                </StackPanel>-->
            </ScrollViewer>
        </Grid>
        <Grid Grid.Row="2" Height="64">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Margin="0,0,1,0"
                  Background="{StaticResource App_Theme_Color}">
                <TextBlock FontSize="22" Foreground="#ffffffff" 
                           Text="重置" Margin="18,0,18,0"
                           VerticalAlignment="Center"/>
            </Grid>
            <Grid Grid.Column="1" Background="{StaticResource App_Theme_Color}">
                <TextBlock Grid.Column="1" FontSize="22" Foreground="#ffffffff" Margin="10,0" VerticalAlignment="Center">
                    <Run  Text="共"/>
                    <Run x:Name="searchResultSeriesCount" Text="999"/>
                    <Run Text="个车系"/>
                    <Run x:Name="searchResultSpecCount" Text="9999"/>
                    <Run Text="个车型符合要求"/>
                </TextBlock>
               
                <Image Source="/Images/ArrowRWhite.png" Stretch="None" HorizontalAlignment="Right" Margin="0,0,12,0"/>

            </Grid>
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>
